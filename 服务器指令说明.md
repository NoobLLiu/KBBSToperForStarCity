# KBBSToperForStarCity 服务器内指令说明

> 插件版本 `3.7.4_A2`（StarCity 定制版）
> 本文档以**实际代码**为准，直接列出服内可用的每一条指令、参数、权限与用法。

---

## 一、指令总览

- 主指令：`/bt`
- 别名：`/poster`（`/kbbstoper` 也可，但配置里主名是 `kbbstoper`，建议用 `/bt`）
- **直接输入 `/bt`（不带任何参数）**：
  - 玩家执行 → 打开主菜单 GUI（可在界面里点按钮操作）
  - 控制台执行 → 显示帮助列表

所有子指令的参数都区分大小写不敏感（会自动转小写/大写处理），但**论坛 ID、玩家名请严格按原样填写**。

---

## 二、玩家可用指令（默认所有玩家都有权限）

| 指令 | 权限节点 | 默认 | 作用 |
| --- | --- | --- | --- |
| `/bt binding <论坛ID>` | `bbstoper.binding` | 所有玩家 | 绑定自己的论坛账号（需输入两次确认） |
| `/bt reward` | `bbstoper.reward` | 所有玩家 | 主动领取自己已顶帖的奖励（走完整发奖逻辑） |

> ⚠️ **本版本不支持"为他人绑定 / 代他人领奖"**。绑定和领取都只作用于执行指令的玩家本人。
> 即：只有 `/bt binding <论坛ID>`（自己），没有 `<玩家>` 参数；`/bt reward` 也没有目标玩家参数。

### 1. 绑定论坛账号
```
/bt binding <论坛ID>
```
- `<论坛ID>` 是 KLPBBS **论坛 ID（用户名）**，不是 uid。
- 首次输入会要求**再输入一次**进行确认（防手滑）；两次一致才写入。
- 若论坛 ID 已被别的玩家绑定 → 提示冲突，本次不绑定。
- 换绑有冷却（`bbs.change-id-cooldown`，默认按天计），冷却内无法换绑。

### 2. 主动领取奖励
```
/bt reward
```
- 仅玩家可用，自动按**自己绑定的论坛账号**去论坛抓取顶帖记录。
- 受查询冷却限制（`bbs.query-cooldown`，默认 30 秒），非 OP 需等冷却结束才能再次触发。
- 有顶帖且未领过 → 发奖并广播；当天次数已用尽 → 提示超出上限；没顶帖 → 提示无记录。

---

## 三、管理员 / OP 指令

以下权限默认归属 OP（`bbstoper.admin` 组包含它们）。

| 指令 | 权限节点 | 默认 | 作用 |
| --- | --- | --- | --- |
| `/bt testreward [normal\|incentive\|offday]` | `bbstoper.testreward` | OP | 对本人生效地测试奖励下发（不发真实抓取，直接跑发奖逻辑） |
| `/bt list [页码]` | `bbstoper.list` | OP | 列出论坛近 6 天内的顶帖者（ID + 顶帖时间） |
| `/bt top [页码]` | `bbstoper.top` | OP | 按顶帖次数排序列出玩家（玩家名 + 论坛ID + 次数） |
| `/bt check bbsid <论坛ID>` | `bbstoper.check` | OP | 查某论坛 ID 被哪个玩家绑定 |
| `/bt check player <玩家ID>` | `bbstoper.check` | OP | 查某玩家绑定的论坛 ID |
| `/bt delete <玩家ID>` | `bbstoper.delete` | OP | 删除某玩家的绑定与顶帖数据 |
| `/bt debug <clear\|status\|simulate>` | `bbstoper.debug` | OP | OP 调试指令（见下方详解） |
| `/bt reload` | `bbstoper.reload` | OP | 热重载配置文件与数据库 |
| `/bt help` | — | 所有 | 按当前权限过滤后显示可见指令 |

> 注：`/bt list`、`/bt top`（以及玩家的 `/bt reward`）都受查询冷却限制；拥有 `bbstoper.bypassquerycooldown` 权限（OP 默认有）可绕过。

### 查询类
```
/bt check bbsid <论坛ID>      # 例如 /bt check bbsid StarCityAdmin
/bt check player <玩家ID>     # 例如 /bt check player Steve
/bt list [页码]               # /bt list 2 看第 2 页
/bt top  [页码]
```

### 数据管理
```
/bt delete <玩家ID>           # 例如 /bt delete Steve（精确 2 个参数，没有 player 子词）
```

### 热重载
```
/bt reload
```
重载后会重新读取 `config.yml` / `message.yml`、重连数据库、重启自动发奖定时器。改配置后无需重启服务端。

---

## 四、OP 调试指令详解（`/bt debug`）

三个子命令**都只对执行指令的 OP 本人生效**，不会误改他人数据。需先绑定才能用。

| 子命令 | 作用 |
| --- | --- |
| `/bt debug clear` | 清空自己当前顶帖状态：重置上次领奖日、今日已领次数、HP 上限（回到基础值 `reward.values.hp-base`），并清除自己的顶帖记录。用于重置测试。 |
| `/bt debug status` | 查看自己当前状态：已绑定论坛 ID、上次领奖日期、今日已领次数、当前 HP 上限。 |
| `/bt debug simulate` | 手动模拟"系统检测到你顶了宣传帖"，走**完整发奖逻辑**（每日配额 / 2 小时间隔仍生效；方便在不真去论坛顶帖的情况下验证奖励是否下发明细）。 |

典型调试流程：
```
/bt debug clear      # 先清干净
/bt debug status     # 确认已清空
/bt debug simulate   # 模拟一次顶帖发奖
/bt debug status     # 看状态变化（已领次数 +1、HP 上限 +2 等）
```

---

## 五、权限节点总表（plugin.yml）

| 权限节点 | 默认 | 说明 |
| --- | --- | --- |
| `bbstoper.user` | 所有玩家 | 用户组，包含 `binding` + `reward` |
| `bbstoper.admin` | OP | 管理组，包含下方所有 OP 权限 + `bypassquerycooldown` |
| `bbstoper.binding` | 所有玩家 | 绑定论坛账号 |
| `bbstoper.reward` | 所有玩家 | 主动领取奖励 |
| `bbstoper.testreward` | OP | 测试奖励下发 |
| `bbstoper.list` | OP | 列出顶帖列表 |
| `bbstoper.top` | OP | 顶贴次数排行 |
| `bbstoper.check` | OP | 查玩家/论坛 ID 绑定 |
| `bbstoper.delete` | OP | 删除玩家数据 |
| `bbstoper.debug` | OP | OP 调试指令 |
| `bbstoper.reload` | OP | 重载插件 |
| `bbstoper.bypassquerycooldown` | OP | 绕过查询冷却 |

> 给某个非 OP 玩家单独授权示例（权限插件里）：
> `lp user Steve permission set bbstoper.list true`

---

## 六、使用范例

**玩家第一次使用**
```
/bt binding StarCityAdmin          # 输入论坛ID
/bt binding StarCityAdmin          # 再输入一次确认
/bt reward                         # 去论坛顶帖后回服领取
```

**OP 排查某玩家**
```
/bt check player Steve             # 看 Steve 绑了哪个论坛ID
/bt top                            # 看谁顶得最多
/bt debug status                   # 看自己测试号状态
```

**改完配置立即生效**
```
/bt reload
```

---

## 七、常见反馈与处理

- **「请先绑定论坛账号」**：执行 `/bt binding <论坛ID>`。
- **「查询冷却中」**：等 `bbs.query-cooldown` 秒，或给执行者 `bbstoper.bypassquerycooldown`。
- **「今日次数已用尽」**：默认每天 `reward.times`（首顶 1 + 额外 2 = 3 次），次日重置。
- **「页面不存在 / 超出页数」**：`/bt list`、`/bt top` 页码超过实际页数。
- **星光点没收到**：检查 Vault + 经济插件是否安装（控制台会有 `Vault 经济核心未安装` 警告）。
- **倍率/HP 没生效**：检查 MGactivity 是否加载、导出接口命令格式是否与 `config.yml` 的 `reward.mgactivity:` 一致（或用 Java API 对接时 MGactivity 是否注册了 `MGactivityApi`）。

---

## 八、与旧版说明的差异（本版已修正）

- `binding` / `reward` **仅本人**：旧文档写的 `/bt bind <论坛ID> [玩家]`、`/bt reward <玩家> [次数]` 在本版代码里**不存在**，已改为仅自己。
- `delete` 实际为 `/bt delete <玩家ID>`（精确 2 参数），不是 `/bt delete player <玩家ID>`。
- 主指令别名 `/bt`、`/poster` 均可；无参数时玩家开 GUI、控制台看帮助。
