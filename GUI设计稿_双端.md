# KBBSToperForStarCity 双端 GUI 设计稿（最终稿 v2）

> 状态：**已确认（2026-08-27）** —— 进入实现。
> 原则：JAVA（Bukkit 箱子）与基岩（Nukkit 表单）**功能完全一致**，交互按双端特点设计；**不使用 PAPI**，全部数据由插件内置解析；**非 OP 功能全部 GUI 化**。

---

## 0. 现状问题（为什么推翻重做）

| 问题 | 现状 |
|---|---|
| 依赖 PAPI | gui.yml 使用 `%player_name%`、`%bbstoper_bbsid%` 等 PAPI 占位符，服务器未装 PAPI 时**原样显示乱码** |
| 功能稀疏 | JAVA 主菜单只有 4 个按钮（绑定/奖励/排行/帖子），无状态区、无子界面、无分页 |
| 基岩表单简陋 | 只有 4 个按钮 + 绑定输入，无我的状态/记录/排行分页/管理菜单 |
| 权限不分 | 排行榜按钮对所有人显示，但实际是 OP 权限，点了报错 |
| 无统一导航 | 子界面无法返回、无关闭习惯、双端布局各自为政 |

---

## 1. 设计原则

1. **同业务、双皮肤**：core 层同一套业务（绑定/领奖/查询/排行），界面层双端各自实现，保证功能一致。
2. **不用 PAPI**：动态数据走插件内置解析（Poster/SQL/`PlaceholderResolver`），内置占位符见 §5。
3. **非 OP 全 GUI**：玩家能做的所有事（绑定、领取、查状态、查记录、看帖、看排行）都在 GUI 内完成；OP 功能收进"管理菜单"（非 OP 不可见）。
4. **分区明确**：信息区 / 功能区 / 查询区 / 管理区，一眼可辨。
5. **双端特点**：
   - JAVA：物品 + 格子 + lore（信息密度高）、头颅皮肤、可点击聊天消息打开外链、翻页按钮。
   - 基岩：大按钮 + 大字、表单无颜色码（自动 strip）、外链只能展示文本供复制、输入用自定义表单、分页用「上一页/下一页」按钮。

---

## 2. 功能矩阵（指令 ↔ GUI，权限分级）

| 功能 | 对应指令 | 权限 | JAVA 入口 | 基岩入口 |
|---|---|---|---|---|
| 打开主菜单 | `/bt` | 玩家 | — | — |
| 绑定 / 更换论坛ID | `/bt binding` | 玩家 | 主菜单[绑定] → 聊天输入+二次确认 | 主表单[绑定] → 输入表单+二次确认 |
| 领取顶帖奖励 | `/bt reward` | 玩家 | 主菜单[领取] | 主表单[领取] |
| 我的状态（绑定ID/次数/HP/今日已领） | —（GUI 展示） | 玩家 | 主菜单信息区 / 状态子页 | 主表单内容区 / 状态子表单 |
| 我的顶帖记录 | `list`（私有化） | 玩家 | 主菜单[我的记录] → 分页子页 | 主表单[我的记录] → 分页子表单 |
| 查看宣传帖（链接+最后顶帖） | —（GUI） | 玩家 | 主菜单[宣传帖] → 可点击聊天消息 | 主表单[宣传帖] → 文本复制 |
| 顶帖排行榜 | `top` | 玩家只读 | 主菜单[排行] → 分页子页 | 主表单[排行] → 分页子表单 |
| 测试奖励 | `testreward` | OP | 管理菜单 → 子菜单选类型 | 管理表单 → 按钮选类型 |
| 顶帖列表（全服） | `list` | OP | 管理菜单[列表] | 管理表单[列表] |
| 检查绑定 | `check` | OP | 管理菜单 → 输入 bbsid/玩家 | 管理表单 → 输入表单 |
| 删除玩家数据 | `delete` | OP | 管理菜单 → 输入玩家名 → 确认 | 管理表单 → 输入表单 |
| 重载插件 | `reload` | OP | 管理菜单[重载] | 管理表单[重载] |
| 调试 | `debug` | OP | 管理菜单 → 子菜单 | 管理表单 → 子表单 |

> 排行榜已确认对玩家开放**只读**（`bbstoper.top` 权限向玩家开放，OP 保留管理）。

---

## 3. JAVA 版（Bukkit 箱子）设计

### 3.1 主菜单（9×4 = 36 格）

```
┌────┬────┬────┬────┬────┬────┬────┬────┬────┐
│ 0  │ 1  │ 2  │ 3  │ 4  │ 5  │ 6  │ 7  │ 8  │
│玩家│边框│边框│边框│边框│边框│边框│边框│关闭│
│头颅│    │    │    │    │    │    │    │    │
│状态│    │    │    │    │    │    │    │    │
├────┼────┼────┼────┼────┼────┼────┼────┼────┤
│ 9  │ 10 │ 11 │ 12 │ 13 │ 14 │ 15 │ 16 │ 17 │
│边框│绑定│边框│领取│边框│我的│边框│宣传│边框│
│    │/换 │    │奖励│    │记录│    │帖子│    │
├────┼────┼────┼────┼────┼────┼────┼────┼────┤
│ 18 │ 19 │ 20 │ 21 │ 22 │ 23 │ 24 │ 25 │ 26 │
│边框│边框│排行│边框│规则│边框│我的│边框│边框│
│    │    │榜  │    │说明│    │状态│    │    │
├────┼────┼────┼────┼────┼────┼────┼────┼────┤
│ 27 │ 28 │ 29 │ 30 │ 31 │ 32 │ 33 │ 34 │ 35 │
│边框│边框│边框│管理│边框│边框│边框│帮助│边框│
│    │    │    │菜单│    │    │    │    │    │
└────┴────┴────┴────┴────┴────┴────┴────┴────┘
```

| 格位 | 物品 | 名称 | Lore（动态数据） | 动作 |
|---|---|---|---|---|
| 0 | 玩家头颅 | `&a我的状态` | 绑定ID / 顶帖次数 / 生命上限 / 今日已领 | 打开[我的状态] |
| 8 | 屏障 | `&c关闭` | — | 关界面 |
| 10 | 命名牌 | 未绑定`&a绑定论坛ID` / 已绑定`&e更换论坛ID` | 当前绑定ID（已绑定时） | 绑定流程 |
| 12 | 向日葵 | `&b领取顶帖奖励` | 今日已领 x/y 次；冷却提示 | `/bt reward` |
| 14 | 书 | `&d我的顶帖记录` | 最近顶帖时间预览 | 打开[我的记录] |
| 16 | 地图 | `&9查看宣传帖` | 帖子状态（最后顶帖） | 发送可点击链接 |
| 20 | 绿宝石 | `&e顶帖排行榜` | 前 3 名预览 | 打开[排行榜] |
| 22 | 附魔书 | `&6活动规则` | 高峰期/每日限制/奖励表摘要 | 打开[规则说明] |
| 24 | 药水 | `&d我的状态`（=0 的快捷入口） | — | 打开[我的状态] |
| 28 | 命令方块 | `&c管理菜单`（仅 OP 可见） | 测试/列表/检查/删除/重载/调试 | 打开[管理菜单] |
| 34 | 书与笔 | `&7帮助` | 指令速查 | 打开[帮助] |

> 边框统一深色玻璃；普通玩家看不到第 4 行的"管理菜单"（该格用边框填充）。
> 所有 `&` 颜色码在构建时由插件转换；动态数据用内置占位符（§5）填充，**不经过 PAPI**。

### 3.2 子界面（JAVA）

| 界面 | 规格 | 内容 |
|---|---|---|
| 我的状态 | 9×3 | 一排信息物品：绑定ID(命名牌)/顶帖次数(纸)/生命上限(红心)/今日已领(烟花)/上次领奖日(时钟)/连签中断(若显示) + 返回 |
| 我的顶帖记录 | 9×6 | 每格一条顶帖记录(纸，显示时间)，最多 45 条/页，底部[上一页][下一页][返回] |
| 排行榜 | 9×6 | 每格一名玩家(头颅+名字+论坛ID+次数)，底部翻页+返回 |
| 规则说明 | 9×3 | 3-4 件信息物品：高峰期/每日配额/附加奖励/奖励表 + 返回 |
| 管理菜单 | 9×3 | 测试奖励/顶帖列表/检查绑定/删除玩家/重载/调试 + 返回（全 OP） |
| 测试奖励子菜单 | 9×3 | normal / incentive / additional 三个按钮 + 返回 |
| 帮助 | 9×3 | 指令列表信息物品 + 返回 |

> 绑定交互（JAVA 特点，已确认）：点[绑定] → 打开**铁砧界面**（自定义 AnvilHolder 伪铁砧，玩家在改名槽输入论坛ID，点结果槽确认）→ 弹出二次确认（沿用 CLI cache 机制）。铁砧改名槽无法限制字符，确认时做白名单校验（仅字母/数字/中文，长度上限），非法输入提示重来。

---

## 4. 基岩版（Nukkit 表单）设计

> 特点：标题/内容无颜色码（自动 strip）；按钮列表式；输入用自定义表单；外链展示文本供复制。

### 4.1 主表单（FormWindowSimple）

```
┌──────────────────────────────┐
│  KBBSToperForStarCity        │
│──────────────────────────────│
│ 论坛ID   : Steve (未绑定)    │
│ 顶帖次数 : 12                │
│ 生命上限 : 32                │
│ 今日已领 : 1/3 次            │
│──────────────────────────────│
│ [ 1. 绑定/更换论坛ID ]        │
│ [ 2. 领取顶帖奖励 ]          │
│ [ 3. 我的顶帖记录 ]          │
│ [ 4. 我的状态 ]              │
│ [ 5. 顶帖排行榜 ]            │
│ [ 6. 查看宣传帖 ]            │
│ [ 7. 管理菜单 (OP可见) ]     │
└──────────────────────────────┘
```

### 4.2 子表单

| 表单 | 类型 | 内容 |
|---|---|---|
| 绑定输入 | 自定义表单 | 输入框输入论坛ID → 二次确认输入（复用 CLI 二次确认） |
| 我的状态 | 信息表单 | 绑定ID/次数/生命上限/今日已领/上次领奖日 纯文本 |
| 我的记录 | 信息表单 | 顶帖时间列表（每行一条），超长时[上一页][下一页] |
| 排行榜 | 信息表单 | 每行：名次+名字+论坛ID+次数，翻页按钮 |
| 宣传帖 | 信息表单 | 链接文本 + 最后顶帖时间（提示：长按复制） |
| 规则说明 | 信息表单 | 高峰期/每日配额/附加奖励/奖励表文本 |
| 管理菜单 | 按钮表单 | 测试奖励(normal/incentive/offday 二级)/列表/检查/删除/重载/调试/返回 |
| 检查/删除输入 | 自定义表单 | 输入框收集参数后执行 |

> 基岩版无"返回"键——**所有子表单末尾都放一个「返回主菜单」按钮**（信息表单可加一个"关闭"按钮）。
> 输入类一律用自定义表单（基岩无法用聊天补全，这是与 JAVA 的交互差异点）。

---

## 5. 内置占位符（不用 PAPI，插件自身解析）

| 占位符 | 含义 | 数据源 |
|---|---|---|
| `%BBSNAME%` | 绑定论坛ID | Poster |
| `%POSTTIMES%` | 顶帖次数 | Poster.topStates |
| `%MAXHP%` | 生命上限 | Poster.maxhp |
| `%REWARDTIME%` | 今日已领次数 | Poster.rewardtime |
| `%REWARDBEFORE%` | 上次领奖日 | Poster.rewardbefore |
| `%LASTPOST%` | 最后顶帖时间 | 网页抓取（仅点击时取，避免卡 GUI） |
| `%TODAYLIMIT%` | 今日上限(首顶+额外) | config |
| `%COOLDOWN%` | 剩余冷却 | CLI.queryrecord |
| `%PAGEURL%` | 宣传帖链接 | config |

> 现有 `PlaceholderResolver`（bbsid/posttimes/pageurl/lastpost/top_N）继续复用，GUI 层新增以上占位符解析入口（core 提供统一 `GuiDataResolver`）。

---

## 6. 双端一致性清单

- [x] 同一功能矩阵（§2）
- [x] 主菜单信息区展示相同字段
- [x] 绑定二次确认逻辑一致（CLI cache 共用）
- [x] 排行榜/记录分页规则一致（每页条数 JAVA 45 / 基岩按行数，可配置）
- [x] 管理功能（OP）入口一致
- [x] 权限过滤一致（OP 项对普通玩家隐藏）

---

## 7. 已确认决策（2026-08-27）

| # | 决策项 | 结论 |
|---|---|---|
| 1 | 排行榜权限 | **玩家只读可见**（`bbstoper.top` 向玩家开放只读，OP 保留管理） |
| 2 | 我的顶帖记录 | **新增**玩家个人记录页（分页）；`list` 全服记录仍归 OP 管理菜单 |
| 3 | 绑定交互（JAVA） | **铁砧界面输入**（自定义 AnvilHolder 伪铁砧 + 改名槽 + 二次确认）；基岩版仍用自定义表单输入 |
| 4 | 活动规则说明页 | **加**（高峰期/每日限制/奖励表） |
| 5 | 语言文件 | 新增 `gui2.*`/`form2.*` 键，重构 `gui.yml` 为配置驱动 |
| 6 | 主菜单尺寸 | JAVA 9×4（36 格），普通玩家看到 9×3（OP 行隐藏） |

---

## 8. 实施范围（确认后执行）

- core：新增 `GuiDataResolver`（内置占位符）、`MenuRouter`（子界面路由状态）、GUI 动作枚举统一。
- bukkit：重构 `GUI`/`GUIManager`，重写 `gui.yml` 为分区布局；新增子界面构建器（状态/记录/排行/规则/管理/帮助）；新增 `AnvilHolder` 铁砧绑定界面。
- nukkit：扩展 `FormRouter` 为多级表单路由；新增各子表单构建器。
- lang.yml / 两份 plugin.yml 不变（plugin 名仍 KBBSToper）。
